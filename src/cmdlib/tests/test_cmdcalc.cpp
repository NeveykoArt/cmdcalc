#include <cmdlib/calculator.hpp>
#include <cmdlib/parser.hpp>
#include <gtest/gtest.h>

TEST(calctest, CmdCalcTest1) {
  try {

  } catch (const std::exception &e) {
    std::cerr << e.what() << "\n";
  };
}
